<%= simple_form_for @cocktail do |f| %>
  <%= f.input :name %>
  <%= f.button :submit, "Create Coktail!", class: "btn-primary" %>
  <%= link_to "Back", cocktails_path(@cocktail), class: "btn btn-primary" %>
<% end %>
